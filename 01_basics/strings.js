const url = "https://cloud.tentoro.in/tentoro%20cloud";

console.log(url.replace("%20", "-"));
console.log(url.includes("areeb"));

const name = "areeb";
const repoCount = 20;
console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);
